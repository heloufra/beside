var express = require('express');
var router = express.Router();
var inboxController  = require("../controllers/inboxController");

//router.get('/', inboxController.inboxView);

module.exports = router;